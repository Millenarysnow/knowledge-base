version: '3.8'

services:
  ollama:
    image: ollama/ollama:latest
    container_name: kb-ollama
    restart: unless-stopped
    ports:
      - "11434:11434"
    volumes:
      - ./data/ollama:/root/.ollama
    environment:
      - OLLAMA_HOST=0.0.0.0
      - OLLAMA_KEEP_ALIVE=24h

  anythingllm:
    image: mintplexlabs/anythingllm:latest
    container_name: kb-anythingllm
    restart: unless-stopped
    ports:
      - "8301:3001"
    volumes:
      - ./data/anythingllm:/app/server/storage
    environment:
      - STORAGE_DIR=/app/server/storage
      - LLM_PROVIDER=ollama
      - OLLAMA_BASE_PATH=http://ollama:11434
      - OLLAMA_MODEL_PREF=${OLLAMA_MODEL:-qwen2.5:7b}
      - EMBEDDING_ENGINE=ollama
      - EMBEDDING_BASE_PATH=http://ollama:11434
      - EMBEDDING_MODEL_PREF=${EMBEDDING_MODEL:-bge-m3}
      - SERVER_NAME=智能知识库
      - LANGUAGE=zh-CN
      - DISABLE_TELEMETRY=true
    depends_on:
      - ollama

  kb-worker:
    image: python:3.11-slim
    container_name: kb-worker
    working_dir: /app
    restart: "no"
    volumes:
      - ./:/app
    environment:
      - ANYTHINGLLM_BASE_URL=http://anythingllm:3001
      - ANYTHINGLLM_API_KEY=${ANYTHINGLLM_API_KEY:-}
    command: >
      bash -c "pip install --no-cache-dir -r requirements.txt && apt-get update -qq && apt-get install -y -qq libreoffice-writer >/dev/null 2>&1 || true && python -m kb.cli init && tail -f /dev/null"
    depends_on:
      - anythingllm

  nginx:
    image: nginx:alpine
    container_name: kb-nginx
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./data/site:/usr/share/nginx/html:ro
      - ./data/documents:/usr/share/nginx/html/files:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
    depends_on:
      - kb-worker
